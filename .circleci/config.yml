version: 2.1

orbs:
  # Add Orb here
  ruby: circleci/ruby@1.8.0


jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      # Add step below to install Ruby version 2.6.3
      - ruby/install:
          version: '2.6.3'

workflows:
  run-jobs:
    jobs:
      - build